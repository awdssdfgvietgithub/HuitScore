
import 'package:huit_score/model/foot/PeriodModel.dart';

class StatisticsModel {
  String? periodName = 'N/a';
  List<GroupModel>? groupsList = [];

  // PeriodModel? all;
  // PeriodModel? oneST;
  // PeriodModel? twoND;

  // StatisticsModel({this.all, this.oneST, this.twoND});
  StatisticsModel({this.periodName, this.groupsList});
}
